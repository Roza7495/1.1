
#include <iostream>
using namespace std;
int main()
{
	int a,b, c, d, n,i,ch;
	cout << "enter a,b,c,d,n" << endl;
	cout << " a<b;c<d;1<=a,b,c,d<=n; 1<=n<=1000" << endl;
	cin >> a >> b >> c >> d >> n;
	int mas[n];
	for (i = 0;i < n;i++)
		a[i] = i;
	for (i = 0;i < n;i++)
		cout << mas[i] << " " << endl;
	for (i = a;i < b;i++)
	{
		ch = a[i];
		a[i]=a[b-1]
		a[i+1] = a[i];
		a[b - 1] = ch;
	}
	for (i = c;i < d;i++)
	{
		ch = a[i];
		a[i] = a[d-1];
		a[i + 1] = a[i];
		a[d - 1] = ch;
	}
	for (i = 0;i < n;i++)
		cout << mas[i] << " " << endl;
}
